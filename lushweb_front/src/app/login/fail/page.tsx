import React, { type ReactElement } from 'react';
import LoginFailTemplate from '@/templates/login/fail';

export default function OrderFailPage(): ReactElement {
	return (
		<>
			<LoginFailTemplate />
		</>
	);
}
